@model PagedList.IPagedList<Advocacia.Models.Mapping.Finalidades>
@{
    ViewBag.Title = "Postagens";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2 class="text-black">Finalidades</h2>
<a class="text-black" href="/Postagens/Categorias">voltar</a>
<p class="text-black">@ViewBag.nomeCategoria / </p>
<hr />
<div class="d-flex flex-wrap mt-5">

    @if (Model.Count > 0)
    {
        foreach (var finalidade in Model)
        {
            <a class="mr-4 text-black" href="@Url.Action("Postagens", "Postagens", new { idFinalidade = finalidade.id,nomeFinalidade = finalidade.descricao })">@finalidade.descricao</a>
        }
    }
</div>
<hr class="mt-5" />
<nav aria-label="Page navigation mt-5">
    <ul class="pagination justify-content-end" style="height: 20px;">
        <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
            <a class="page-link" href="@Url.Action("Finalidades", "Postagens", new { page = Model.PageNumber - 1 })" tabindex="-1">Anterior</a>
        </li>

        @for (int i = 1; i <= Model.PageCount; i++)
        {
            <li class="page-item @(Model.PageNumber == i ? "active" : "")">
                <a class="page-link" href="@Url.Action("Finalidades", "Postagens", new { page = i })">@i</a>
            </li>
        }

        <li class="page-item @(Model.PageNumber == Model.PageCount ? "disabled" : "")">
            <a class="page-link" href="@Url.Action("Finalidades", "Postagens", new { page = Model.PageNumber + 1 })">Próximo</a>
        </li>
    </ul>
</nav>
<script>
    var usuarioAdm = "@ViewBag.UsuarioAdm"
</script>
